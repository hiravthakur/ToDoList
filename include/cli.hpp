#ifndef CLI_HPP
#define CLI_HPP
#include "ToDoList.hpp"

//The menu diplayed in the CLI
void mainMenu();

//The input handler is a testing and user view harness
void inputHandler(ToDoList& inLst);

#endif
